# MySQL — Railway interno (após adicionar plugin MySQL no projeto)
# Copie os valores em: serviço MySQL → Variables no painel Railway
MYSQL_HOST=${{MySQL.MYSQLHOST}}
MYSQL_PORT=${{MySQL.MYSQLPORT}}
MYSQL_USER=${{MySQL.MYSQLUSER}}
MYSQL_PASSWORD=${{MySQL.MYSQLPASSWORD}}
MYSQL_DATABASE=${{MySQL.MYSQLDATABASE}}

# Supabase
SUPABASE_URL=https://bhargdkruycbrcanfvuz.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGci...  # chave service_role (nunca expor no frontend)
